import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.PWAWeatherApp',
  appName: 'PWAWeatherApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
